
version: 2

models:
    - name: raw_to_formatted
      description: "Raw to Formatted"
      columns:
          - name: id
            description: "The primary key for this table"
            tests:
                - unique
                - not_null

